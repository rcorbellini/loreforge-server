"""O Motor / fisica (pacote, item 31 §3) — domínio puro (primitivas).

Sem executores (nenhum @registro.handler): só primitivas. O __init__ reexporta a
API por NOME para `from ..fisica import X` e `motor.fisica.X` seguirem iguais.
"""
from .primitivas import (  # noqa: F401
    CARRY_KG_PER_STR,
    DEAD,
    DEFAULT_BODY_WEIGHT_KG,
    DEFAULT_WEIGHT_BY_SIZE,
    DOWN_CONDITIONS,
    HAND_SLOT,
    IMPROVISED_ATTRIBUTE,
    IMPROVISED_DAMAGE,
    INCAPACITATED,
    MotorError,
    PUSH_IMPOSSIBLE_RATIO,
    PUSH_KG_PER_STR,
    PUSH_TRIVIAL_RATIO,
    Path,
    _CROSSING_BY_SIZE,
    _CUSTOS_FADIGA,
    _TETO_SONO_PLENO_S,
    _char_fm,
    _conditions_of,
    _direct_items,
    _fail,
    _inside_closed,
    _is_wide_open,
    _marca,
    _reachable_item_folder,
    _slot_cap,
    _slot_pega,
    _walk_open_items,
    annotations,
    apply_damage,
    apply_healing,
    apply_speed,
    attr_modifier,
    body_of,
    body_weight_of,
    carried_item_ids,
    carried_weight,
    carry_capacity,
    check_carga,
    check_carga_livre,
    check_empurrar,
    check_encaixe,
    check_mao,
    check_slot,
    check_vaga,
    open_container_for,
    default_hp_max,
    effective_weight,
    ensure_fatigue,
    ensure_hp,
    fatigue_max,
    find_character_folder,
    finish_rest,
    grasp_slot_of,
    has_accessible_item,
    io,
    is_closed,
    is_currency,
    is_dead,
    is_down,
    is_for_sale,
    is_negotiable,
    hunger_label,
    fatigue_label,
    is_resting,
    item_own_weight,
    item_size,
    item_slot,
    porter_of,
    protection_of,
    push_capacity,
    read_doc,
    rest_fraction,
    rolagem,
    slot_capacity,
    slots_in_use,
    speed_multiplier_of,
    spend_fatigue,
    custo_da_falha,
    CUSTO_TENTATIVA_SOCIAL,
    start_rest,
    total_value,
    total_weight_of,
    value_of,
    weapon_of,
    write_doc,
)
